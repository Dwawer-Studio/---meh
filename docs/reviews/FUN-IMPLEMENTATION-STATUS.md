# تنفيذ النقلة النوعية — متابعة الإثبات

الخطة المرجعية: `2026-09-05-FUN-REVIEW.md`. بداية التنفيذ: 2026-09-06.
طلب المالك: تنفيذ A وB وC كاملة، ثم تجربة ومحاكاة من الوكيل قبل تسليمها.

الحالة: اكتمل تنفيذ A+B+C والتحقق الهندسي في 2026-09-06. اعتماد المتعة البشري
يبقى مفتوحًا، لا يُستبدل بمحاكاة أو ضغط الوكيل زر الإعادة. لا نشر عام ضمن التسليم.

| المتطلب | الدفعة | الحالة | دليل الإغلاق المطلوب |
|---|---|---|---|
| F01 فحص كل بطاقة وأثرها/ثمنها قبل اللعب، هوية اختيار ثابتة | A | منفذ ومتحقق هندسيًا | 17 اختبار واجهة، منها فحص غير الصالح، وإلغاء/تأكيد واختيار ثابت؛ 8 أحجام شاشة |
| F02 سجل سببي صحيح لكل حركة وتأثير وعقوبة | A | منفذ ومتحقق هندسيًا | قاموس AR/EN، عقوبة متراكمة، نتيجة القرار، ملخص باقٍ، سجل خدمة من أحداث ملتزمة فقط |
| F03 قرارات باقية واضحة، هدف/قيمة/تحذير الفوز | A | منفذ ومتحقق هندسيًا | بوشلاخ/الحرباية/أفضل واحد/الألوان عبر واجهة حقيقية؛ تحقق PeerJS وانتهاء قرار الخدمة |
| F07 حالات قوى/درع/آخر بطاقة مرئية | A | منفذ ومتحقق هندسيًا | عرض محلي + إسقاط حالات عامة للخادم/PeerJS مع رفض المؤشرات الفاسدة |
| F10 الآرت محفوظ مع إعادة تحميل حقيقية | A | منفذ ومتحقق هندسيًا | كل البصمات المقفولة مطابقة؛ اعتراض طلب الصورة وإعادتها ناجح بلا Emoji بديل |
| F04 سياسة خصوم عادلة سياقية وأساليب مختلفة | B | منفذ ومتحقق هندسيًا | `FUN-B-EVIDENCE.md`: حالات تكتيكية ومنع قراءة الخاص ومحاكاة 1000 مباراة مقارنة |
| F06 ضغط الانتظار بلا حذف معنى أو تغيير قاعدة | B | منفذ ومتحقق هندسيًا | في المتصفح 6676→1884 ms لنفس ثلاث الحركات؛ بصمة نهائية مطابقة |
| F05 تدريب معلن بثلاثة مواقف من نفس القواعد | C | منفذ ومتحقق هندسيًا | مواقف حقيقية، إعادة/تجاوز، لا نتائج أو تاماشي، ثم 4/7/60 |
| F08 نتيجة ذات معنى ومنافسة محلية متتابعة | C | منفذ ومتحقق هندسيًا | لحظة من الجولة، أربع نتائج، إعادة إلى الجولة 2؛ لا ادعاء سببية الفوز |
| F09 إيقاف الفردي وخروج مؤكد وإعدادات وحفظ/استئناف | C | منفذ ومتحقق هندسيًا | إعادة تحميل وسط بوشلاخ/الحرباية/اللون؛ إيقاف/إعدادات/خروج؛ فشل تخزين وتعارض تبويبين |
| F11 اختبارات تكامل حقيقية + تجربة غير موجهة محفوظة | C | الهندسي مكتمل؛ البشري معلّق | 8 اختبارات C ومباراة عادية حقيقية؛ الورقة في `docs/FINAL-PLAYTEST.md` بلا بيانات بشرية مختلقة |
| F12 قياس واضح بموافقة وحماية التاماشي/الأعلام | C | منفذ ومتحقق هندسيًا | لا جمع قبل الموافقة وبعد سحبها؛ تصدير محلي يدوي؛ فصل التدريب والاستعادة؛ الاقتصاد لم يتغير |
| الكور والآرت والتوافق AR/EN/لمس/كيبورد/حركة مخففة | الكل | متحقق هندسيًا | 290 اختبارًا و90 متصفحًا؛ البصمات المقفولة مطابقة؛ الأجهزة وقارئات الشاشة الفعلية في السجل المؤجل |
| كوميت مستقل بعد كل دفعة | الكل | محفوظ على `codex/fun-overhaul` | A: `ed2928f`، B: `3859991`، C: الكوميت المتضمن هذا السجل بعنوان `feat(fun-c)` |
| نسخة نهائية قابلة للتجربة | الكل | جاهزة محليًا | `http://127.0.0.1:4173/`؛ البناء ناجح؛ النشر العام بموافقة منفصلة |

التحقق البشري المقترح في الخطة يبقى مسجلًا، ولا يُستبدل بادعاء أن محاكاة البوتات تمثل 12 لاعبًا. التنفيذ وتجربة الوكيل لا ينتظران تجنيد مشاركين؛ المالك سيجرب بعد اكتمال النسخة حسب طلبه.

## سجل الدفعة A

الكوميت: `ed2928f` على `codex/fun-overhaul`، رُفع إلى origin بنجاح. ليس نشرًا للنسخة العامة.

- `node --test`: 278/278 ناجح في الفحص الشامل، ثم اختبار إضافي لانتهاء قرار الخادم ضمن 21/21 فحصًا مركزًا ناجحًا. يعاد الفحص الشامل عند اكتمال العمل.
- `tests/e2e/uiux-u3.spec.js`: 17/17 ناجح. لا نعتبر هذا مباراة بشرية ولا إثبات متعة.
- `npm run validate`: HTML وCSS ناجحان.
- تكامل الشبكة: الستة نجحت في المرور السابق؛ في آخر مرور نجحت خمسة وتعثر اختبار الدعوة المنتهية بمهلة عامة، ثم نجح منفردًا (7.3 ثانية) دون تعديل أو زيادة المهلة. يبقى استقرار المرور الكامل ضمن إعادة الفحص النهائية، ولا يُخفى التعثر.
- فحص الكور: 1000 مباراة محلية ببذور ثابتة، مرتان لكل بذرة؛ نفس trace والنتيجة، مع حفظ 60 بطاقة فريدة.
- راجعت صورة الهاتف الحقيقية من اختبار الواجهة: `artifacts/fun-a-portrait-inspection.png`. الآرت الأصلي ظاهر كاملًا، والشرح وأزرار القرار داخل الشاشة.
- اللمس: عندما يصبح التراكب حاجبًا لمراكز البطاقات، تتحول اليد إلى صف قابل للتمرير بأهداف لمس منفصلة. لا إعادة رسم أو قص للبطاقات.
- السجل العام للخادم يحتفظ بآخر 20 انتقالًا في الذاكرة (حتى 128 مباراة)، وهو واجهة عرض فقط، لا سجل مالي ولا حفظ دائم. إعادة تشغيل الخدمة تمسح هذا الجزء الحديث؛ لا يغيّر إعادة تشغيل القواعد أو حفظ المباراة. لا تتضمن بياناته هويات البطاقات المخفية أو تفاصيل البطاقة المتبرع بها.
- إخفاقات أثناء التطوير لم تُخفَ: توقعات قديمة لتعطيل زر البطاقة استُبدلت بفحص قابليتها للفحص مع منع اللعب؛ تراكب لمس حقيقي أُصلح؛ تعارض منفذ عند تشغيل مجموعات متوازية عولج بتشغيلها تسلسليًا؛ مهلة فحص واحدة أثناء ضغط الفحوص لم تتكرر في إعادة المجموعة كاملة.
- حالة النشر: لم تُحدّث النسخة العامة. يعمل المحلي فقط حتى طلب النشر.

## سجل الدفعة B

الكوميت: `3859991`؛ رُفع إلى origin بنجاح.

- `node --test --test-concurrency=4`: 286/286 ناجح؛ لا تخطٍّ ولا اختبارات معطلة.
- فحص البصمات المقفولة وHTML/CSS ناجح.
- الدليل المفصل والمقارنة وحدود الاستنتاج: `FUN-B-EVIDENCE.md`.
- إدارة الجلسة والحفظ والتدريب والنتيجة والقياس ما زالت C؛ لم تُغلق بإنجاز B.

## سجل الدفعة C والإغلاق الهندسي

دليل التنفيذ وحدوده: `FUN-C-EVIDENCE.md`. التسليم في كوميت مستقل
بعنوان `feat(fun-c): add optional practice, resumable solo sessions and consented evidence`.

- آخر `node --test --test-concurrency=4`: **290/290**، صفر تخطٍّ أو تعطيل، 21.1 ثانية.
- آخر `npx playwright test` كاملًا: **90/90**، 3.6 دقيقة، صفر retries، ودون
  `--update-snapshots`. يشمل الخدمة وPeerJS والمتجر وكل مراحل UIX والصور وaxe
  و4x CPU، وكل بطاقات اليد أهداف نقر فعلية بلا `force` عبر مصفوفة الأجهزة.
- الكور: 1000 مباراة ببذور ثابتة مرتين لكل بذرة، مع تطابق الإعادة وحفظ 60 بطاقة؛
  عقد الآرت والملفات المقفولة كلها ناجحة. لم تتغير بصمات الحماية أو مصادرها الثلاثة.
- HTML وCSS والصياغة والبناء ناجحة؛ تحققت مطابقة **56 ملف JS/CSS** في حزمة
  `dist/client` للمصدر. تعليق بناء معزول أُوقف ثم أعيد بإذن بناء محلي وأنهى بالرمز 0؛
  ليس نشرًا ولا ادعاء نجاح للمحاولة المعلقة.
- آخر مباراة عادية عبر الأزرار: **52,643 ms**، 18 إدخالًا، فوز خالد، الأيدي
  [5،3،3،0]، 60 هوية فريدة، صفر أخطاء متصفح؛ ثم إعادة صحيحة. لا تُحسب لاعبًا بشريًا.
- المرور الشامل الأول لم يُخفَ: 78/90؛ ست صور مرجعية قديمة، وخمسة فحوص لم
  تراعِ صف التمرير، وتجاوز أثر 53ms أثناء توزيع جارٍ. عولج عيب بطاقة دخول التدريب
  وقص رأس النتيجة، ورُوجعت الست صور وتحدّثت فقط لعرض A/B/C المقصود. فحص
  التمرير أضيف إليه نقر فعلي على البطاقات السبع؛ وفحص الأثر ينتظر اكتمال توزيع
  البداية ثم يقيسه مستقلًا. لم تُرفع حدود 50ms/100ms أو نسب اختلاف الصور.
- لقطات `artifacts/fun-*` هي آخر مرور؛ اللقطات التاريخية الأصلية محفوظة في كوميتاتها.
- لم يُغيّر الرابط العام. تجربة المالك والقياس البشري القصير هما الخطوة التالية،
  لا خطوة هندسية إضافية مخترعة ولا ضمان ترند.
